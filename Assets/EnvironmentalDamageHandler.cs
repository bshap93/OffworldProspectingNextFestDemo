using UnityEngine;

public class EnvironmentalDamageHandler : MonoBehaviour
{
    [Header("Damage Tracking")] [SerializeField]
    private bool debugLogDamage;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    private void Start()
    {
    }

    // Update is called once per frame
    private void Update()
    {
    }
}